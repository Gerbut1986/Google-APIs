@model string
@Model

    <body>
        <p>@ViewBag.body</p>
        <ul>
            @for(int i = 0; i < ViewBag.name.Length; i++)
            {
                <li><a asp-action="" href="#">@ViewBag.name[i]</a></li>
            }
        </ul>
    </body>